// Layout components — header, sidebar, main layout wrapper
// Import here, never import individual files directly

// (futuro) export { AppLayout } from './AppLayout';
// (futuro) export { Header } from './Header';
// (futuro) export { Sidebar } from './Sidebar';
