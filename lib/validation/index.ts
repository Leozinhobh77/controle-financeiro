// Zod validation schemas — tipagem de validação em runtime
// ALWAYS import from this file, never from individual schema files

// Exemplo: import { schemaConta } from '@/lib/validation';

// (futuro) export { schemaConta } from './conta';
// (futuro) export { schemaConfig } from './config';
